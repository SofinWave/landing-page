# Review package: 0cf3080..HEAD

## Commits
2ad0520 feat(ui): add HudCard primitive

## Files changed
 components/hud-card.tsx            | 17 +++++++++++++++++
 tests/components/hud-card.test.tsx | 11 +++++++++++
 2 files changed, 28 insertions(+)

## Diff
diff --git a/components/hud-card.tsx b/components/hud-card.tsx
new file mode 100644
index 0000000..fc0ae5f
--- /dev/null
+++ b/components/hud-card.tsx
@@ -0,0 +1,17 @@
+import type { ComponentProps } from "react";
+import { Card } from "@/components/ui/card";
+import { cn } from "@/lib/utils";
+
+export function HudCard({ className, children, ...props }: ComponentProps<typeof Card>) {
+  return (
+    <Card
+      className={cn(
+        "relative overflow-hidden transition-shadow hud-corners hover:glow-accent",
+        className,
+      )}
+      {...props}
+    >
+      {children}
+    </Card>
+  );
+}
diff --git a/tests/components/hud-card.test.tsx b/tests/components/hud-card.test.tsx
new file mode 100644
index 0000000..0de41e2
--- /dev/null
+++ b/tests/components/hud-card.test.tsx
@@ -0,0 +1,11 @@
+import { render, screen } from "@testing-library/react";
+import { describe, it, expect } from "vitest";
+import { HudCard } from "@/components/hud-card";
+
+describe("HudCard", () => {
+  it("renders children and hud-corners class", () => {
+    const { container } = render(<HudCard>content</HudCard>);
+    expect(screen.getByText("content")).toBeInTheDocument();
+    expect(container.querySelector(".hud-corners")).not.toBeNull();
+  });
+});
