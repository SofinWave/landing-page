# Review: Task 17 (2da8a0b..HEAD)

## Commits
d604b91 feat(home): add footer

## Files
 app/[locale]/(public)/home/_components/footer.tsx | 21 +++++++++++++++++++++
 messages/en.json                                  |  6 ++++++
 messages/vi.json                                  |  6 ++++++
 tests/sections/footer.test.tsx                    | 20 ++++++++++++++++++++
 4 files changed, 53 insertions(+)

## Diff
diff --git a/app/[locale]/(public)/home/_components/footer.tsx b/app/[locale]/(public)/home/_components/footer.tsx
new file mode 100644
index 0000000..16ef43f
--- /dev/null
+++ b/app/[locale]/(public)/home/_components/footer.tsx
@@ -0,0 +1,21 @@
+import { useTranslations } from "next-intl";
+
+export function SiteFooter() {
+  const t = useTranslations("footer");
+  return (
+    <footer className="border-t border-border bg-muted/30">
+      <div className="container mx-auto flex flex-col items-center justify-between gap-4 px-4 py-10 sm:flex-row">
+        <div>
+          <div className="font-semibold">{t("brand")}</div>
+          <div className="text-sm text-muted-foreground">{t("tagline")}</div>
+        </div>
+        <a href={`mailto:${t("email")}`} className="text-sm text-muted-foreground hover:text-foreground">
+          {t("email")}
+        </a>
+        <div className="text-sm text-muted-foreground">
+          © 2026 {t("brand")}. {t("rights")}
+        </div>
+      </div>
+    </footer>
+  );
+}
diff --git a/messages/en.json b/messages/en.json
index bfbb551..42e0e5d 100644
--- a/messages/en.json
+++ b/messages/en.json
@@ -128,12 +128,18 @@
   "contact": {
     "title": "Let's talk about your system",
     "subtitle": "Tell us what you're building. We'll reply within one business day.",
     "name": "Name",
     "email": "Email",
     "message": "Message",
     "submit": "Send message",
     "sending": "Sending…",
     "success": "Thanks — we'll be in touch shortly.",
     "error": "Something went wrong. Please try again."
+  },
+  "footer": {
+    "brand": "kingnnt.org",
+    "tagline": "Software consulting & implementation.",
+    "email": "hello@kingnnt.org",
+    "rights": "All rights reserved."
   }
 }
diff --git a/messages/vi.json b/messages/vi.json
index c15c662..32e25da 100644
--- a/messages/vi.json
+++ b/messages/vi.json
@@ -128,12 +128,18 @@
   "contact": {
     "title": "Cùng trao đổi về hệ thống của bạn",
     "subtitle": "Hãy cho chúng tôi biết bạn đang xây gì. Chúng tôi phản hồi trong một ngày làm việc.",
     "name": "Họ tên",
     "email": "Email",
     "message": "Nội dung",
     "submit": "Gửi tin nhắn",
     "sending": "Đang gửi…",
     "success": "Cảm ơn bạn — chúng tôi sẽ liên hệ sớm.",
     "error": "Có lỗi xảy ra. Vui lòng thử lại."
+  },
+  "footer": {
+    "brand": "kingnnt.org",
+    "tagline": "Tư vấn & triển khai phần mềm.",
+    "email": "hello@kingnnt.org",
+    "rights": "Bảo lưu mọi quyền."
   }
 }
diff --git a/tests/sections/footer.test.tsx b/tests/sections/footer.test.tsx
new file mode 100644
index 0000000..9fc25ff
--- /dev/null
+++ b/tests/sections/footer.test.tsx
@@ -0,0 +1,20 @@
+import { describe, it, expect } from "vitest";
+import { render, screen } from "@testing-library/react";
+import { NextIntlClientProvider } from "next-intl";
+import en from "@/messages/en.json";
+import { SiteFooter } from "@/app/[locale]/(public)/home/_components/footer";
+
+describe("SiteFooter", () => {
+  it("renders brand and contact email", () => {
+    render(
+      <NextIntlClientProvider locale="en" messages={en}>
+        <SiteFooter />
+      </NextIntlClientProvider>,
+    );
+    expect(screen.getByText("Software consulting & implementation.")).toBeInTheDocument();
+    expect(screen.getByRole("link", { name: "hello@kingnnt.org" })).toHaveAttribute(
+      "href",
+      "mailto:hello@kingnnt.org",
+    );
+  });
+});
