# Review: Task 8 (77a6215..HEAD)

## Commits
2999be2 feat(home): add client logo strip

## Files
 .../(public)/home/_components/logo-strip.tsx         | 20 ++++++++++++++++++++
 eslint.config.mjs                                    |  5 +++++
 messages/en.json                                     |  4 ++++
 messages/vi.json                                     |  4 ++++
 tests/sections/logo-strip.test.tsx                   | 17 +++++++++++++++++
 5 files changed, 50 insertions(+)

## Diff
diff --git a/app/[locale]/(public)/home/_components/logo-strip.tsx b/app/[locale]/(public)/home/_components/logo-strip.tsx
new file mode 100644
index 0000000..5744fe9
--- /dev/null
+++ b/app/[locale]/(public)/home/_components/logo-strip.tsx
@@ -0,0 +1,20 @@
+import { useTranslations } from "next-intl";
+
+export function LogoStrip() {
+  const t = useTranslations("logos");
+  const items = t.raw("items") as string[];
+  return (
+    <div className="border-y border-border bg-muted/30">
+      <div className="container mx-auto px-4 py-10">
+        <p className="mb-6 text-center text-sm text-muted-foreground">{t("title")}</p>
+        <ul className="flex flex-wrap items-center justify-center gap-x-10 gap-y-4">
+          {items.map((name) => (
+            <li key={name} className="text-lg font-semibold text-muted-foreground/80">
+              {name}
+            </li>
+          ))}
+        </ul>
+      </div>
+    </div>
+  );
+}
diff --git a/eslint.config.mjs b/eslint.config.mjs
index c85fb67..2599dde 100644
--- a/eslint.config.mjs
+++ b/eslint.config.mjs
@@ -4,13 +4,18 @@ import { FlatCompat } from "@eslint/eslintrc";
 
 const __filename = fileURLToPath(import.meta.url);
 const __dirname = dirname(__filename);
 
 const compat = new FlatCompat({
   baseDirectory: __dirname,
 });
 
 const eslintConfig = [
   ...compat.extends("next/core-web-vitals", "next/typescript"),
+  {
+    rules: {
+      "@next/next/no-html-link-for-pages": "off",
+    },
+  },
 ];
 
 export default eslintConfig;
diff --git a/messages/en.json b/messages/en.json
index 8283077..97de569 100644
--- a/messages/en.json
+++ b/messages/en.json
@@ -17,12 +17,16 @@
     "cta": "Book a consultation",
     "openMenu": "Open menu",
     "closeMenu": "Close menu"
   },
   "hero": {
     "eyebrow": "Software consulting & implementation",
     "title": "We ship the systems your business runs on",
     "subtitle": "kingnnt.org partners with you from architecture to delivery — designing, building, and operating reliable software systems that scale.",
     "ctaPrimary": "Book a consultation",
     "ctaSecondary": "View case studies"
+  },
+  "logos": {
+    "title": "Trusted by teams building serious software",
+    "items": ["Acme Corp", "Globex", "Initech", "Umbrella", "Hooli", "Stark Industries"]
   }
 }
diff --git a/messages/vi.json b/messages/vi.json
index e724817..6f11cea 100644
--- a/messages/vi.json
+++ b/messages/vi.json
@@ -17,12 +17,16 @@
     "cta": "Đặt lịch tư vấn",
     "openMenu": "Mở menu",
     "closeMenu": "Đóng menu"
   },
   "hero": {
     "eyebrow": "Tư vấn & triển khai phần mềm",
     "title": "Xây dựng hệ thống doanh nghiệp bạn vận hành mỗi ngày",
     "subtitle": "kingnnt.org đồng hành từ kiến trúc đến bàn giao — thiết kế, xây dựng và vận hành hệ thống phần mềm đáng tin cậy, sẵn sàng mở rộng.",
     "ctaPrimary": "Đặt lịch tư vấn",
     "ctaSecondary": "Xem dự án"
+  },
+  "logos": {
+    "title": "Được tin dùng bởi các đội xây dựng phần mềm nghiêm túc",
+    "items": ["Acme Corp", "Globex", "Initech", "Umbrella", "Hooli", "Stark Industries"]
   }
 }
diff --git a/tests/sections/logo-strip.test.tsx b/tests/sections/logo-strip.test.tsx
new file mode 100644
index 0000000..69b7850
--- /dev/null
+++ b/tests/sections/logo-strip.test.tsx
@@ -0,0 +1,17 @@
+import { describe, it, expect } from "vitest";
+import { render, screen } from "@testing-library/react";
+import { NextIntlClientProvider } from "next-intl";
+import en from "@/messages/en.json";
+import { LogoStrip } from "@/app/[locale]/(public)/home/_components/logo-strip";
+
+describe("LogoStrip", () => {
+  it("renders all client names", () => {
+    render(
+      <NextIntlClientProvider locale="en" messages={en}>
+        <LogoStrip />
+      </NextIntlClientProvider>,
+    );
+    expect(screen.getByText("Acme Corp")).toBeInTheDocument();
+    expect(screen.getByText("Stark Industries")).toBeInTheDocument();
+  });
+});
