# Review package: afcdd4a..HEAD

## Commits
4212c87 feat(home): system status line in footer

## Files changed
 app/[locale]/(public)/home/_components/footer.tsx | 4 ++++
 messages/en.json                                  | 1 +
 messages/vi.json                                  | 1 +
 tests/sections/footer.test.tsx                    | 9 +++++++++
 4 files changed, 15 insertions(+)

## Diff
diff --git a/app/[locale]/(public)/home/_components/footer.tsx b/app/[locale]/(public)/home/_components/footer.tsx
index 68c9ecf..5d8e236 100644
--- a/app/[locale]/(public)/home/_components/footer.tsx
+++ b/app/[locale]/(public)/home/_components/footer.tsx
@@ -1,20 +1,24 @@
 import { useTranslations } from "next-intl";
 
 export function SiteFooter() {
   const t = useTranslations("footer");
   return (
     <footer className="border-t border-border bg-muted/30">
       <div className="container mx-auto flex flex-col items-center justify-between gap-4 px-4 py-10 sm:flex-row">
         <div>
           <div className="font-semibold">{t("brand")}</div>
           <div className="text-sm text-muted-foreground">{t("tagline")}</div>
+          <div className="mt-2 inline-flex items-center gap-2 font-mono text-xs text-muted-foreground">
+            <span className="h-2 w-2 rounded-full bg-primary shadow-[0_0_8px_var(--accent-glow)]" />
+            {t("status")}
+          </div>
         </div>
         <a
           href={`mailto:${t("email")}`}
           className="text-sm text-muted-foreground hover:text-foreground"
         >
           {t("email")}
         </a>
         <div className="text-sm text-muted-foreground">
           © 2026 {t("brand")}. {t("rights")}
         </div>
diff --git a/messages/en.json b/messages/en.json
index 4a7bc03..8ea3276 100644
--- a/messages/en.json
+++ b/messages/en.json
@@ -190,14 +190,15 @@
     "email": "Email",
     "message": "Message",
     "submit": "Send message",
     "sending": "Sending…",
     "success": "Thanks — we'll be in touch shortly.",
     "error": "Something went wrong. Please try again."
   },
   "footer": {
     "brand": "kingnnt.org",
     "tagline": "Software consulting & implementation.",
+    "status": "All systems operational",
     "email": "hello@kingnnt.org",
     "rights": "All rights reserved."
   }
 }
diff --git a/messages/vi.json b/messages/vi.json
index ce8f020..5dc38f5 100644
--- a/messages/vi.json
+++ b/messages/vi.json
@@ -193,14 +193,15 @@
     "email": "Email",
     "message": "Nội dung",
     "submit": "Gửi tin nhắn",
     "sending": "Đang gửi…",
     "success": "Cảm ơn bạn — chúng tôi sẽ liên hệ sớm.",
     "error": "Có lỗi xảy ra. Vui lòng thử lại."
   },
   "footer": {
     "brand": "kingnnt.org",
     "tagline": "Tư vấn & triển khai phần mềm.",
+    "status": "Hệ thống đang vận hành",
     "email": "hello@kingnnt.org",
     "rights": "Bảo lưu mọi quyền."
   }
 }
diff --git a/tests/sections/footer.test.tsx b/tests/sections/footer.test.tsx
index 9fc25ff..73402f9 100644
--- a/tests/sections/footer.test.tsx
+++ b/tests/sections/footer.test.tsx
@@ -10,11 +10,20 @@ describe("SiteFooter", () => {
       <NextIntlClientProvider locale="en" messages={en}>
         <SiteFooter />
       </NextIntlClientProvider>,
     );
     expect(screen.getByText("Software consulting & implementation.")).toBeInTheDocument();
     expect(screen.getByRole("link", { name: "hello@kingnnt.org" })).toHaveAttribute(
       "href",
       "mailto:hello@kingnnt.org",
     );
   });
+
+  it("renders the system status line", () => {
+    render(
+      <NextIntlClientProvider locale="en" messages={en}>
+        <SiteFooter />
+      </NextIntlClientProvider>,
+    );
+    expect(screen.getByText("All systems operational")).toBeInTheDocument();
+  });
 });
