#!/usr/bin/env bash
#MISE description="Rebuild and restart Raspberry Pi containers"
set -euo pipefail
export COMPOSE_FILE="docker-compose.yml:docker-compose.rpi.yml"
[[ -f .env.rpi ]] && ENV_FILE="--env-file .env.rpi" || ENV_FILE=""
docker compose ${ENV_FILE} up -d --build
